export const limits = {
    storage: 1000, //storage
    textLen: 1000, // message text length
    pTextLen: 500, //post text length
    folderLimit: 30, //folder limit
    tLimit: 4096, //tribe member capacity
    uTLimit: 100, //tribe limit
    cLimit: 512, //connections limit
    nLimit: 30, //notes limit
};